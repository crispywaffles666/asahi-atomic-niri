# These package hooks write to /var but ship no tmpfiles rules. bootc lint needs
# rules that can rebuild each path at boot.
d /var/lib/blueman 0755 root root - -
d /var/lib/power-profiles-daemon 0755 root root - -
